describe('UPLOAD CSV endpoint', () => {
    test('Writes rows to db', () => {})
})

describe('GET CSV endpoint', () => {
    describe('Returns csv name, data, rowCount etc', () => {})
    describe('Returns a limited number of rows', () => {})
})