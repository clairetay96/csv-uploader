describe('Home Page', () => {
    describe('When the page first loads', () => {
        describe('Renders input', () => {})
        describe('Renders submit button', () => {})

    })

    describe('When a file is uploaded', () => {
        describe('Displays an upload progress bar', () => {})
        describe('On success, redirects to CSV rows page', () => {})
    })
})