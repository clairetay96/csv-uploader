describe('Csv Rows Page', () => {
    describe('When there is no data present', () => {
        describe('Shows loading spinner', () => {})

    })
    describe('When there is data present', () => {
        describe('Renders all the rows returned in the request', () => {})
        describe('Renders pagination buttons', () => {})
        describe('Renders CSV name', () => {})
        describe('Renders Search input', () => {})
    })
})